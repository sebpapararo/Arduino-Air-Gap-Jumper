# Arduino-Air-Gap-Jumper

This repo contains all the code for using an Arduino (with either IR or RF) to send and receive data contained within a text file on the host device to a corresponding receiver. This was my MSc dissertation.
